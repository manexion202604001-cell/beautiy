# Supabase 接続情報（Supabaseダッシュボード → Settings → API から取得）
# .env.local にコピーして値を設定する。service_role キーは絶対にここへ書かない。
VITE_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOi...

# デモ配信（GitHub Pages等）用ビルド時のみ 1 を設定
# VITE_DEMO_HASH_ROUTER=1
